# The image builds with this app's own root as the context (see docker/Dockerfile), so these must be
# excluded here — the monorepo's root .dockerignore does not apply to this build.
node_modules
dist
.vite
*.local
.DS_Store
.git
